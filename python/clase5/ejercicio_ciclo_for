# ejercicio ciclo for
numbers = [1, 2, 3, 5]  # Aun con la lista vacía se va a ejecutar el else
for n in numbers:
    print(n)
    if n == 3:
        break  # Esta es la única manera para que no se ejecute el else
else:
    print("Esto se terminó")